#include <iostream>
using namespace std;
//here we use count variable to check the number...
//if its not 1 then it means it can be in other number table also..
//so not a prime
int main(){
	int count,j,i,n;
    cin>>n;
    for(i=2;i<=n;i++)
    {
        count=0;
        for(j=2;j<=i;j++)
        {
            if(i%j==0)
                count++;
        }
        if(count==1)
        {
            cout<<i;
            cout<<endl;
        }
    } 
}


