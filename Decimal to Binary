#include<iostream>
using namespace std;

int main() {
	int rem,n;
    long ans=0,i;
    cin>>n;
    // i is increasing every time by 10 so v need to store it in long data type
    for(i=1;n!=0;i=i*10)
    {
        rem=n%2;
        n=n/2;
          ans=ans+rem*i;
    }
    cout<<ans;
}
